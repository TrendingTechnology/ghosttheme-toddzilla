{{!

    PAGINATION
    - User configuration of this file is not required.

}}

    <nav class="pagination">
        {{#if next}}
            <a href="{{page_url next}}" class="largeoutline dark wide right">
                <span class="main"><i class="fa fa-arrow-right" aria-hidden="true"></i></span>
                <span class="sub"><i class="fa fa-chevron-right"></i></span>
            </a>
        {{/if}}
        {{#if prev}}
            <a href="{{page_url prev}}" class="largeoutline dark wide left">
                <span class="main"><i class="fa fa-arrow-left" aria-hidden="true"></i></span>
                <span class="sub"><i class="fa fa-chevron-left"></i></span>
            </a>
        {{/if}}
    </nav>
